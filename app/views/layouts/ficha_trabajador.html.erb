<!DOCTYPE html>
<html>
  <head>
    <title>Codelco</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-fluid">

      <%= render 'navbar' %>

      <p id="notice"><%= notice %></p>

      <div class="card">
      <div class="card-body">

<!-- Muestra datos del trabajador a través de partial, controler debe proveer instancia @trabajador -->
      <h4>Datos del trabajador</h4>
      <%= render 'trabajadors/trabajadoractual' %>

      <%= button_to "Testeo y búsqueda activa", new_trabajador_test_path(@trabajador), method: 'get', form_class: "btn ", class: "btn btn-info" %>

      <table class = "table">
        <thead>
          <tr>
            <th>  <%= link_to "Ver resultados de testeo", trabajador_tests_path(@trabajador), method: 'get'  %></th>
            <th>  <%= link_to "Ver episodios anteriores", trabajador_episodes_path(@trabajador), method: 'get'  %></th>
          </tr>
        </thead>
      </table>

      </div>
    </div>

    <div class="card">
    <div class="card-body">

      <div id="content"><%= content_for?(:nuevoEpisodio) ? yield(:nuevoEpisodio) : nil %></div>

      <div id="content"><%= content_for?(:EpisodioActual) ? yield(:EpisodioActual) : nil %></div>

      <div id="content"><%= content_for?(:nuevoTest) ? yield(:nuevoTest) : nil %></div>

      <div id="content"><%= content_for?(:nuevoSintoma) ? yield(:nuevoSintoma) : nil %></div>

      <div id="content"><%= content_for?(:nuevoContacto) ? yield(:nuevoContacto) : nil %></div>

      <div id="content"><%= content_for?(:modificarEpisodio) ? yield(:modificarEpisodio) : nil %></div>

      <div id="content"><%= content_for?(:nuevoSeguimiento) ? yield(:nuevoSeguimiento) : nil %></div>

      <%= yield %>

    <div id="content"><%= content_for?(:resultadosTesteo) ? yield(:resultadosTesteo) : nil %></div>

    <table class = "table">
      <thead>
        <tr>
          <% if @episode == nil %>
          <% else %>
          <% if @episode.id == nil %>
          <% else %>
          <th>  <%= link_to "Detalles del Episodio actual", episode_path(@episode), method: 'get'  %></th>
          <th>  <%= link_to "Ver seguimientos anteriores", episode_seguimientos_path(@episode), method: 'get'  %></th>
          <% end %>
          <% end %>

        </tr>
      </thead>
    </table>

  </div>
</div>



    </div>


  </body>
</html>
